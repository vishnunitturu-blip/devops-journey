# Day 22 – Docker Compose

Docker Compose is used to manage multiple Docker containers.

## Key Points
- Uses docker-compose.yml
- Runs multiple services
- Single command to start or stop apps

Command:
docker compose up

